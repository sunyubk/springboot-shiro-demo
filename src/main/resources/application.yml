# 应用服务 WEB 访问端口
server:
  port: 9090
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://123.57.45.239:3306/test?serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&useSSL=false
    username: root
    password: root

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:/mapper/*.xml

shiro:
  enabled: true
#  密码算法名称
  hashAlgorithmName: MD5
#  哈希迭代次数
  hashIterations: 1024

